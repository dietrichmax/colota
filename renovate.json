{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "postUpdateOptions": ["npmDedupe"],
  "packageRules": [
    {
      "description": "Bump react-native and its @react-native/* tooling in one PR.",
      "groupName": "react-native monorepo",
      "matchPackageNames": [
        "react-native",
        "@react-native/babel-preset",
        "@react-native/eslint-config",
        "@react-native/jest-preset",
        "@react-native/metro-config",
        "@react-native/typescript-config"
      ]
    },
    {
      "description": "React Native pins its own toolchain and nests private copies of it, so bumping these independently gains nothing and drifts the app off the only version set RN tests. Update them together with react-native, taking the versions from the RN upgrade helper template.",
      "matchFileNames": ["apps/mobile/package.json", "package.json"],
      "matchPackageNames": [
        "@babel/**",
        "typescript",
        "jest",
        "@types/jest",
        "eslint",
        "@typescript-eslint/**",
        "metro**"
      ],
      "enabled": false
    }
  ]
}
