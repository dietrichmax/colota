name: Docs Check
permissions:
  contents: read

on:
  pull_request:
    branches: [main]
    paths:
      - 'apps/docs/**'
      - 'packages/shared/**'
      - '.github/workflows/docs-check.yml'
  workflow_dispatch:

jobs:
  build:
    name: Build Docs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared package
        run: npm run build -w @colota/shared

      - name: Build docs
        run: npm run build -w @colota/docs
